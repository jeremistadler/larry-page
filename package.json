{
    "private": true,
    "workspaces": [
        "backend",
        "frontend",
        "shared",
        "worker",
        "wasm-worker"
    ],
    "scripts": {
        "deploy": "cd ./frontend && yarn build && cd ../backend && wrangler publish"
    }
}